import L from 'leaflet';

/**
 * Provides `Leaflet` implementation of required internal methods,
 * pertaining to projection and coordinate reference systems.
 * 
 * &nbsp;
 * 
 * @module protoCRS
 */
export const protoCRS = L.CRS;