import { util, Map, factory } from '../../core';
import { snap } from '..';

export const line = {
    ...snap,
    shape: 'line',
    options: {},
    enabled: false,
    _doesSelfIntersect: false,




};